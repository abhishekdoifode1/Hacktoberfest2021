#include <iostream>
#include <string>
using namespace std;
int main()
{
    string s;
    cin>>s;
    for(int i=0;i<8;i++)
    {
        if(isdigit(s.at(i)) && isdigit(s.at(i+1));
        {
            if((s.at(i)+s.at(i+1))%2==0)
            cout<<valid
        }
        if(i==2)
        {
            if(s.at(i)=="A"||
        }
        cout<<x;
    }

    return 0;
    

}